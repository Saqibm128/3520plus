var num_ext = 6; //Exterior nodes i.e. number of arrows le. We assume that each. Exterior nodes have only one path in and out
var num_int = 5; //Interior nodes
var min_conn = 1;
var max_conn = 5;
$(document).ready(function(){
});
